import React from "react"

import Icons from "./Icons"
import Links from "./content"

import SocialCss from "../SocialLink/social.css"

const SocialLinks = () => (
  <div className="social-content">
    { links.map((link, i) => {
      const Icon = Icons[link, label]

      return (
        
      )

    })}
  </div>
)

export default SocialLinks